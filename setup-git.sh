#!/bin/bash

# Скрипт для настройки Git и подключения к GitHub

echo "🚀 Настройка Git репозитория..."

# Инициализация репозитория
if [ ! -d ".git" ]; then
    echo "📦 Инициализация Git репозитория..."
    git init
else
    echo "✅ Git репозиторий уже инициализирован"
fi

# Проверка удаленного репозитория
if git remote | grep -q "origin"; then
    echo "✅ Удаленный репозиторий уже добавлен"
    git remote -v
else
    echo "🔗 Добавление удаленного репозитория..."
    git remote add origin https://github.com/SipanBabajanyan/Subscripton-Offer-System.git
    echo "✅ Удаленный репозиторий добавлен"
fi

# Добавление всех файлов
echo "📝 Добавление файлов..."
git add .

# Проверка статуса
echo ""
echo "📊 Статус репозитория:"
git status

# Создание коммита (если есть изменения)
if ! git diff --cached --quiet; then
    echo ""
    echo "💾 Создание коммита..."
    git commit -m "Initial commit: Subscription Payment Timeline System

- V2: Чистый HTML/CSS/JS версия с современным дизайном
- Акцент на оплатах (1, 2, 3 оплата)
- Gate для сдачи проекта
- Subscription + Maintenance после получения проекта
- Современный UI/UX дизайн
- Адаптивная верстка для всех устройств"
    echo "✅ Коммит создан"
else
    echo "ℹ️  Нет изменений для коммита"
fi

# Переименование ветки в main
echo ""
echo "🌿 Настройка ветки main..."
git branch -M main 2>/dev/null || echo "Ветка уже main"

# Инструкции для отправки
echo ""
echo "✅ Настройка завершена!"
echo ""
echo "📤 Для отправки кода на GitHub выполните:"
echo "   git push -u origin main"
echo ""
echo "Если возникнет ошибка аутентификации, используйте Personal Access Token"
echo "или настройте SSH ключи."


